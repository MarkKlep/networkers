# Same value as blog/client/.env's REACT_APP_GOOGLE_CLIENT_ID - the client and
# this service must agree on which app a Google token was issued for.
# See CLAUDE.md > Authentication for how to get one.
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
