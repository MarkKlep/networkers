# Same value as auth/.env's GOOGLE_CLIENT_ID - the client and that service
# must agree on which app a Google token was issued for.
# See CLAUDE.md > Authentication for how to get one.
REACT_APP_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
